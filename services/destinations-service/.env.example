# Copy to .env for local (non-Docker) runs.

PORT=4002
CORS_ORIGIN=*

# This service owns the destinations catalog AND the user reviews - both
# live in this one file (the reviews array alongside destinations).
DB_FILE=./data/destinations.json
